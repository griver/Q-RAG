defaults:
  - envs: musique_deep
  - algo: pqn_qwen
  - logger: logging
  - _self_

seed: 42
device: 'cuda:0'
learning_start: 200
steps_count: 1000
batch_size: 12
accumulate_grads: 8
eval_interval: 100
eval_episodes: 300
envs_parallel: 3
max_state_length: 512
max_action_length: 110
    # "state": 512,
    # "action": 110