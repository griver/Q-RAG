defaults:
  - envs: babilong 
  - algo: pqn
  - logger: tb
  - _self_

seed: 42
device: 'cuda:1'
learning_start: 200
steps_count: 1_000_000
batch_size: 30
log_interval: 100
eval_interval: 20
eval_episodes: 100
