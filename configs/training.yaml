defaults:
  - envs: babilong 
  - algo: pqn
  - logger: logging
  - _self_

seed: 42
device: 'cuda:0'
learning_start: 200
steps_count: 50_000
batch_size: 32
eval_interval: 20
eval_episodes: 100
envs_parallel: 3
max_state_length: 512
max_action_length: 64
