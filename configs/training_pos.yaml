defaults:
  - envs: babilong 
  - algo: pqn_pos
  - logger: tb
  - _self_

seed: 42
device: 'cuda:0'
buffer_size: 32
learning_start: 200
steps_count: 1_000_000
update_every: 32
batch_size: 16
log_interval: 100
eval_interval: 1000
eval_episodes: 100
positional_coding: enum #options: rope | enum | none
